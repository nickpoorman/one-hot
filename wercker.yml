box: zhevron/iojs
build:
  steps:
    - script:
        name: Use io.js 2.2.x
        code: |
          nvm use iojs-v2.2
          echo "node version $(node -v) running"
          echo "npm version $(npm -v) running"
    - npm-install
    - npm-test
